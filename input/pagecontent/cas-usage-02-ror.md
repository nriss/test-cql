### Cas d'usage 02 — ROR

> **À venir** : ce cas d'usage appliquera des règles CQL sur des données ROR réelles (`Organization`, `Location`, `HealthcareService`).

#### Exemples de règles envisagées

- Proportion d'organisations actives sans adresse renseignée (qualité des données)
- Proportion d'établissements sanitaires sans `HealthcareService` associé
- Détection d'incohérences entre `Organization.active` et les `Location` rattachées
